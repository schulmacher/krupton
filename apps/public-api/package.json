{
  "name": "@krupton/public-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "concurrently -k -n server,typecheck -c cyan,red \"tsx watch src/index.ts --clear-screen=false\" \"tsc --noEmit --watch --preserveWatchOutput\"",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "test": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@krupton/config": "workspace:*",
    "@krupton/api-interface": "workspace:*",
    "fastify": "^4.28.1",
    "ws": "^8.18.0"
  }
}


