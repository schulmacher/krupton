{
  "name": "@krupton/market-data-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev:fetcher": "concurrently -k -n server,typecheck -c cyan,red \"tsx watch src/mdsFetcher.ts --clear-screen=false\" \"tsc --noEmit --watch --preserveWatchOutput\"",
    "dev:storage": "concurrently -k -n server,typecheck -c cyan,red \"tsx watch src/mdsStorage.ts --clear-screen=false\" \"tsc --noEmit --watch --preserveWatchOutput\"",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "test": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@krupton/config": "workspace:*",
    "@krupton/api-interface": "workspace:*",
    "@krupton/service-framework-node": "workspace:*",
    "@krupton/utils": "workspace:*",
    "ws": "^8.18.0"
  }
}


