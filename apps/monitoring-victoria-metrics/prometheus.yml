# Prometheus scrape configuration for VictoriaMetrics
# VictoriaMetrics supports Prometheus scrape config format

global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: 'external-bridge-fetcher'
    static_configs:
      - targets: ['localhost:3000']
        labels:
          service: 'external-bridge'
          component: 'fetcher'
          environment: 'development'

  - job_name: 'external-bridge-storage'
    static_configs:
      - targets: ['localhost:3001']
        labels:
          service: 'external-bridge'
          component: 'storage'
          environment: 'development'

  - job_name: 'external-bridge-websockets'
    static_configs:
      - targets: ['localhost:3002']
        labels:
          service: 'external-bridge'
          component: 'websockets'
          environment: 'development'

  - job_name: 'victoriametrics'
    static_configs:
      - targets: ['localhost:8428']
        labels:
          service: 'victoriametrics'
          environment: 'development'
